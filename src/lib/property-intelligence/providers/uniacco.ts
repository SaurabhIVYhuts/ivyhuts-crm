import { createNotImplementedAdapter } from "./notImplemented";

export const uniaccoAdapter = createNotImplementedAdapter(
  "uniacco",
  "No Phase 1 audit data for UniAcco; no real integration built in this repo — see README.md."
);

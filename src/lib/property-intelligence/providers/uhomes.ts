import { createNotImplementedAdapter } from "./notImplemented";

export const uhomesAdapter = createNotImplementedAdapter(
  "uhomes",
  "No Phase 1 audit data for UHomes; no real integration built in this repo — see README.md."
);

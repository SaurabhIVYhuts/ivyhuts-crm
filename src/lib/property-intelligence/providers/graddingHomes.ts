import { createNotImplementedAdapter } from "./notImplemented";

export const graddingHomesAdapter = createNotImplementedAdapter(
  "gradding_homes",
  "No Phase 1 audit data for Gradding Homes; no real integration built in this repo — see README.md."
);

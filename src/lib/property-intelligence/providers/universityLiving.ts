import { createNotImplementedAdapter } from "./notImplemented";

export const universityLivingAdapter = createNotImplementedAdapter(
  "university_living",
  "No Phase 1 audit data for University Living; no real integration built in this repo — see README.md."
);
